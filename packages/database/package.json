{
  "name": "@fiscalzen/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./schema": {
      "import": "./dist/schema/index.js",
      "types": "./dist/schema/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts src/schema/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts src/schema/index.ts --format esm --dts --watch --silent",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/migrate.ts",
    "db:push": "drizzle-kit push --force",
    "db:push:test": "drizzle-kit push --config drizzle-test.config.ts --force",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed.ts",
    "db:seed:dev": "tsx src/seed-dev.ts --allow-dev",
    "db:test": "tsx src/test-connection.ts",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist drizzle"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "drizzle-kit": "^0.31.8",
    "tsup": "^8.0.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
